-- I tried this a bit and didn't find all its options even working. It seems
-- like I already have most of the stuff I care about anyway, so I'm putting it
-- on the disabled list.
return {
	"tummetott/unimpaired.nvim",
	event = "VeryLazy",
	opts = {
	},
}
